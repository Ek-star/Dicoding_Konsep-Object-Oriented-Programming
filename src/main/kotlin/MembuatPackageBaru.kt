import com.dicoding.oop.utils.*
import kotlin.system.exitProcess

fun main() {
    // Untuk membuat Packages baru, kita hanya perlu klik kanan pada folder setelah main, kemudian arahkan pada menu "New  > Package"

    sayHello()
    println(factorial(4.0))
    println(pow(3.0, 2.0))
    println(PI)
    println(areaOfCircle(7.2))
}